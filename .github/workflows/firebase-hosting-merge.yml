# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Deploy to Firebase Hosting on merge
'on':
  push:
    branches:
      - main
env:
  FB_PROJECT_ID: '${{ vars.FB_PROJECT_ID }}'
  FB_CLIENT_EMAIL: '${{ vars.FB_CLIENT_EMAIL }}'
  FB_PRIVATE_KEY: '${{ vars.FB_PRIVATE_KEY }}'
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: echo "Deploying to Firebase Hosting on merge...$FB_PROJECT_ID"
      - run: npm ci && npm run build
      - uses: FirebaseExtended/action-hosting-deploy@v0
        with:
          repoToken: '${{ secrets.GITHUB_TOKEN }}'
          firebaseServiceAccount: '${{ secrets.FIREBASE_SERVICE_ACCOUNT_AI_TESTING_396020 }}'
          channelId: live
          projectId: ai-testing-396020
